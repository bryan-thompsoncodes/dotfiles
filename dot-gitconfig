[user]
	name = Bryan Thompson

[commit]
	gpgsign = true

[core]
	excludesfile = ~/.gitignore_global
	fileMode = false
	hooksPath = ~/.git-hooks

[credential]
	helper = cache --timeout=3600

[push]
	autoSetupRemote = true
[branch]
	autosetuprebase = always
	rebase = true

# Include specific git configs from local machines based on repo's origin
# These files are .gitignored
[includeIf "hasconfig:remote.origin.url:*github.com*"]
	path = ~/.gitconfig.github
[includeIf "hasconfig:remote.origin.url:*git.snowboardtechie.com*"]
	path = ~/.gitconfig.snowboardtechie
