# Linux-specific Alacritty configuration
# Import base config
import = ["alacritty-base.toml"]

# Override window decorations for Linux
[window]
decorations = "Buttonless"  # Provides title bar for window management on GNOME

